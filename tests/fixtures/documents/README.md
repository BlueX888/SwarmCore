# Document intake demo fixtures

这些文件用于验证通用业务资料接入，不包含业务硬编码说明。

- `demo-contract.txt`：可抽取标题/主体/金额的文本合同样例
- `demo-integrity.txt`：完整性校验样例
- 可用同目录脚本生成 DOCX/XLSX/PDF（见 `generate_demo_files.py`）

用户验收时只需在业务资料库或多业务工作台上传这些文件，无需编辑 JSON。
