[project]
name = "swarmcore-repository-verifier"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[project.scripts]
swarmcore-repository-verify = "swarmcore_repository_verifier.main:run"

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
