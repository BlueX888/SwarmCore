[project]
name = "swarmcore-runtime-temporal"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["temporalio==1.15.0"]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
