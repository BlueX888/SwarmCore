[project]
name = "swarmcore-spec"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "pydantic==2.11.7",
  "pyyaml==6.0.2",
]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

