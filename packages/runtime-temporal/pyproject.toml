[project]
name = "swarmcore-runtime-temporal"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["swarmcore-spec", "temporalio==1.15.0"]

[tool.uv.sources]
swarmcore-spec = { workspace = true }

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
